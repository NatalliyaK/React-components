import { render, screen } from '@testing-library/react';
import ReactHooksForm from './ReactHooksForm';


describe('ReactHooksForm', () => {

  it('passing test', () => {
    expect(true).toBeTruthy();
  })

  it('failing test', () => {
    expect(false).toBeTruthy();
  })

})
